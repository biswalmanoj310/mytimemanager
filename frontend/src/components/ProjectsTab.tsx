/**
 * Projects Tab Component
 * Extracted from Tasks.tsx to reduce file size
 * Handles project list, project details, and task management
 */

import React, { useState } from 'react';

interface ProjectsTabProps {
  // Props will be added as we extract
}

export default function ProjectsTab(props: ProjectsTabProps) {
  return (
    <div className="projects-container">
      <p>Projects tab - under construction</p>
    </div>
  );
}
